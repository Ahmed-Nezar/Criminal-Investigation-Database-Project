
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage, ttk, Scrollbar
from tmp_data import *


OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"assets/frame4")

def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


window = Tk()

window.geometry("766x508")
window.configure(bg = "#FFFFFF")


canvas = Canvas(
    window,
    bg = "#FFFFFF",
    height = 508,
    width = 766,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
image_image_1 = PhotoImage(
    file=relative_to_assets("image_1.png"))
image_1 = canvas.create_image(
    383.0,
    254.0,
    image=image_image_1
)

button_image_1 = PhotoImage(
    file=relative_to_assets("button_1.png"))
button_1 = Button(
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    command=lambda: print("button_1 clicked"),
    relief="flat",
    activebackground="#000000",
    bg="#000000"
)
button_1.place(
    x=530.0,
    y=33.0,
    width=229.0,
    height=49.000003814697266
)

image_image_2 = PhotoImage(
    file=relative_to_assets("image_2.png"))
image_2 = canvas.create_image(
    172.0,
    57.0,
    image=image_image_2
)

image_image_3 = PhotoImage(
    file=relative_to_assets("image_3.png"))
image_3 = canvas.create_image(
    388.0,
    309.0,
    image=image_image_3
)

table = ttk.Treeview(master=window, columns=table_columns, show="headings")

for column in table_columns:
    table.heading(column=column, text=column)
    table.column(column=column, width=238) # can be changed to fit the window

for row_data in table_data:
    table.insert(parent="", index="end", values=row_data)
    

style = ttk.Style()
style.theme_use("default")

# Set background, foreground, and fieldbackground to lighter shades of grey
style.configure("Treeview", background="#DDDDDD", fieldbackground="#DDDDDD", foreground="black")

# Set background, foreground, and fieldbackground of headings to lighter shades of grey
style.configure("Treeview.Heading", background="#CCCCCC", fieldbackground="#CCCCCC", foreground="black")

# Set background of selected items to a shade of grey
style.map("Treeview", background=[("selected", "#AAAAAA")])

table.place(x=30, y=150, height=320) # can be changed to fit the window

# Create a vertical scrollbar
v_scrollbar = Scrollbar(window, orient="vertical", command=table.yview)
v_scrollbar.place(x=20 + 238 * len(table_columns), y=150, height=320)
# Configure the scrollbar to scroll the treeview
table.configure(yscrollcommand=v_scrollbar.set)

# Place the table on the window

image_image_4 = PhotoImage(
    file=relative_to_assets("image_4.png"))
image_4 = canvas.create_image(
    420.0,
    111.0,
    image=image_image_4
)




window.resizable(False, False)
window.mainloop()
